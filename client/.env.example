# Public URL of the deployed backend API, including the /api suffix.
# Leave empty for local development — Vite's dev-server proxy handles it (see vite.config.ts).
VITE_API_URL=https://your-backend.onrender.com/api
